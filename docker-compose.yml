x-common-config: &common-config
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - ~/.orbitclaw:/root/.orbitclaw

services:
  orbitclaw-gateway:
    container_name: orbitclaw-gateway
    <<: *common-config
    command: ["gateway"]
    restart: unless-stopped
    environment:
      ORBITCLAW_DATA_DIR: /root/.orbitclaw
      # Use Debian system CA bundle for Python requests/urllib3 inside the container.
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
      REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
      CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
    ports:
      - 18790:18790
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  orbitclaw-webui:
    container_name: orbitclaw-webui
    <<: *common-config
    profiles:
      - webui
    command: ["webui", "--host", "0.0.0.0", "--port", "18791"]
    restart: unless-stopped
    environment:
      ORBITCLAW_DATA_DIR: /root/.orbitclaw
      # Optional: set in shell before `docker compose up` to enable Web UI auth.
      ORBITCLAW_WEBUI_PATH_TOKEN: ${ORBITCLAW_WEBUI_PATH_TOKEN:-}
      # Use Debian system CA bundle for Python requests/urllib3 inside the container.
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
      REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
      CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
    ports:
      - 18791:18791
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
        reservations:
          cpus: '0.10'
          memory: 128M
  
  orbitclaw-cli:
    <<: *common-config
    profiles:
      - cli
    command: ["status"]
    environment:
      ORBITCLAW_DATA_DIR: /root/.orbitclaw
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
      REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
      CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
    stdin_open: true
    tty: true
